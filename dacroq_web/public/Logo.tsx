import * as React from "react";

// Fix the TypeScript error by adding a type to props
const SVGComponent = (props: React.SVGProps<SVGSVGElement>) => (
  <svg
    id="Layer_1"
    xmlns="http://www.w3.org/2000/svg"
  />
);